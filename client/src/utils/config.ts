// REPLACE CONFIG:

const auth0config = {
  domain: '',
  client_id: '',
  audience: 'hasura',
};

const hasuraConfig = {
  gqlEndpoint: '',
  wssEndpoint: '',
};

export { auth0config, hasuraConfig };
