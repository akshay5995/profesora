const pathToPage: { [key: string]: string } = {
  '/': 'Home',
  '/stream': 'Stream',
};

const pageToPath: { [key: string]: string } = {
  Home: '/',
  Stream: '/stream',
};

export { pathToPage, pageToPath };
